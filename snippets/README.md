# Snippets

This directory is for copy-paste implementations intended for environments where developers need fast execution with limited governance.

Guidelines:

- Keep snippets framework-agnostic where possible
- Use Rhythm Web tokens and base classes
- Prefer semantic naming over ad hoc styling
- Optimize for quick adoption without inviting design drift
