# Card Snippets

Card patterns should be treated as a family rather than a single implementation.

Initial targets:

- product card
- promo card
- tile and pill variants
- contained cards

When adding snippets, prefer reusable building blocks over tightly coupled one-off compositions.
